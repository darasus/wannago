'use client';

import {Text} from 'ui';

export default function Loading() {
  return (
    <div className="h-screen w-screen flex justify-center items-center">
      <Text>Loading...</Text>
    </div>
  );
}
