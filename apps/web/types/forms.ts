export interface JoinForm {
  email: string;
  firstName: string;
  lastName: string;
  hasPlusOne: boolean;
}

export interface ContactForm {
  subject: string;
  message: string;
  firstName: string;
  lastName: string;
  email: string;
}
