{
  "name": "database",
  "version": "1.0.0",
  "license": "MIT",
  "main": "./index.ts",
  "scripts": {
    "generate": "dotenv -e ./.vercel/.env.development.local -e ./.env -- npx prisma generate --data-proxy",
    "push": "dotenv -e ./.env -- npx prisma db push",
    "studio": "dotenv -e ./.env -- npx prisma studio",
    "lint": "eslint"
  },
  "dependencies": {},
  "devDependencies": {
    "eslint-config-custom": "workspace:*",
    "tsconfig": "workspace:*"
  }
}
