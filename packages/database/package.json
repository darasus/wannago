{
  "name": "database",
  "version": "1.0.0",
  "license": "MIT",
  "main": "./index.ts",
  "scripts": {
    "generate": "dotenv -e ../../.vercel/.env.development.local -- npx prisma generate",
    "push": "dotenv -e ../../.vercel/.env.development.local -- npx prisma db push",
    "studio": "dotenv -e ../../.vercel/.env.development.local -- npx prisma studio",
    "lint": "eslint"
  },
  "dependencies": {},
  "devDependencies": {
    "eslint-config-custom": "workspace:*",
    "tsconfig": "workspace:*"
  }
}
