{
  "name": "database",
  "version": "1.0.0",
  "license": "MIT",
  "main": "./index.ts",
  "scripts": {
    "generate": "dotenv -e ./.vercel/.env.development.local -- npx prisma generate --accelerate",
    "push": "dotenv -e ./.env -- npx prisma db push",
    "studio": "dotenv -e ./.env -- npx prisma studio",
    "lint": "eslint"
  },
  "dependencies": {},
  "devDependencies": {
    "eslint-config-custom": "workspace:*",
    "tsconfig": "workspace:*"
  }
}
